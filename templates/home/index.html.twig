{% extends 'base.html.twig' %}

{% block title %}Accueil - Location de Véhicules{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center">Nos Véhicules Disponibles 🚗</h1>

        <div class="row">
            {% for vehicule in vehicules %}
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        {% if vehicule.image %}
                            <img src="{{ asset('images/' ~ vehicule.image) }}" class="card-img-top" alt="Image du véhicule">
                        {% else %}
                            <img src="{{ asset('images/default-car.jpg') }}" class="card-img-top" alt="Image par défaut">
                        {% endif %}
                        <div class="card-body">
                            <h5 class="card-title">{{ vehicule.marque }}</h5>
                            <p class="card-text">
                                <strong>Immatriculation :</strong> {{ vehicule.immatriculation }}<br>
                                <strong>Prix Journalier :</strong> {{ vehicule.prixjournalier }}€<br>
                                <strong>Disponible :</strong> 
                                {% if vehicule.disponible %}
                                    <span class="badge bg-success">Oui</span>
                                {% else %}
                                    <span class="badge bg-danger">Non</span>
                                {% endif %}
                            </p>
                            <a href="{{ path('app_vehicule_show', {'id': vehicule.id}) }}" class="btn btn-primary">Voir Détails</a>
                        </div>
                    </div>
                </div>
            {% else %}
                <p class="text-center mt-4">Aucun véhicule disponible pour le moment.</p>
            {% endfor %}
        </div>
    </div>
{% endblock %}